# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in formica.gemspec
gemspec

gem 'rake', '~> 10.5'
gem 'rspec', '~> 3.7'
gem 'rubocop', '~> 0.55.0'
